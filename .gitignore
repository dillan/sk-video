node_modules/
dist/
# Built web app bundle (shipped via package.json "files", served by the plugin) — build output, not source.
public/
*.log
.DS_Store
coverage/
# Plugin runtime data (go2rtc binary, config, uploads) lives in the SK data dir, never the repo.
data/
